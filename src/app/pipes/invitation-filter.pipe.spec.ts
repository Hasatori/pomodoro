import { InvitationFilterPipe } from './invitation-filter.pipe';

describe('InvitationFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new InvitationFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
