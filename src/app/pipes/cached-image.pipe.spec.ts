import { CachedImagePipe } from './cached-image.pipe';

describe('CachedImagePipe', () => {
  it('create an instance', () => {
    const pipe = new CachedImagePipe();
    expect(pipe).toBeTruthy();
  });
});
